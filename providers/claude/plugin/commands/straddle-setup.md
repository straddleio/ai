---
description: Set up Straddle MCP server, CLI, and API keys
---

Run the setup skill. Walk through each section, skipping steps already completed. On re-run, check status of each step first and only act on what is missing.
